`
Codewars notes
    -.reverse() is a method that flips an array to its inverse. It is destructive, so be careful`
    const array1 = ['one', 'two', 'three'];
    console.log('array1:', array1);
    // Expected output: "array1:" Array ["one", "two", "three"]
    
    const reversed = array1.reverse();
    console.log('reversed:', reversed);
    // Expected output: "reversed:" Array ["three", "two", "one"]
    
    // Careful: reverse is destructive -- it changes the original array.
    console.log('array1:', array1);
    // Expected output: "array1:" Array ["three", "two", "one"]
    

//HTML
    //Structure
    //Markup language
        //defines where element goes, starts, and stops
        // whitespace is condensed
        //Elements
            // Paragraph tag
                 // <p>This is a tag</p>
            // Image
                // <img>file
        //Attributes
            //Modify an element
            //Behold, a type attribute
                // <input type ="text">  <- default
                // <input type ="password">
                // <input type ="checkbox">
            //Booleans default to fault
                // <input required> is the same as <input required ="false">
<html> 
    <head>   </head>
    <body>   </body>
    </html> 
           
    //id
        //identifies an element
    //class
        //categorizes elements
    //form 
        //does stuff with data
//CSS
    //Style, Layout
   

//JavaScript
    //Interactivity



      